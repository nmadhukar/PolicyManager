# Legacy Agent Folder

The canonical framework lives in `.ai/`.

Use:

- `../AGENTS.md` for global rules.
- `../.ai/agents/` for agent definitions.
- `../.ai/skills/` for reusable procedures.
- `../.ai/commands/` for command workflows.
- `../.ai/tasks/` for task templates and backlog.

This folder is kept only because it already existed in the workspace.
